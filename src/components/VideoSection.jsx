import React from 'react';
import { getDestinationKeyword } from '../utils/helpers'; // This is correct now!

const VideoSection = ({ destination }) => {
  // ... rest of your component code
};

export default VideoSection;